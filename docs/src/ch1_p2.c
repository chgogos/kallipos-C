#include <stdio.h>

int fun(int a, int b) {
  // η παράμετρος b δεν χρησιμοποιείται (εντοπίζεται με το -Wextra)
  return a + 42;
}

int main(void) {
  int x; // μεταβλητή που δεν έχει αρχικοποιηθεί (εντοπίζεται με το -Wall)
  int y = 1729; // μεταβλητή που δεν χρησιμοποιείται (εντοπίζεται με το -Wall)
  int z[0]; // πίνακας με μέγεθος μηδέν (εντοπίζεται με το -pedantic)
            // μεταβλητή που δεν έχει αρχικοποιηθεί (εντοπίζεται με το -Wall)
  printf("x=%d", x);
  return 0;
}
